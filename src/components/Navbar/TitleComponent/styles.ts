import styled from "styled-components";

export const Container = styled.div`
  margin: 24px 0;

  a {
    all: unset;
    font-size: 1.4rem;
    cursor: pointer;
    font-weight: bolder;
    color: black;
  }

  .blog {
    color: #06d6a0;
    font-size: 1.2rem;
    margin-left: 4px;
  }
`;
